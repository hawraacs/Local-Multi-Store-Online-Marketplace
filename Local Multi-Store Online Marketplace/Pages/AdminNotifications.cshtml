@page "/AdminNotifications"
@model Local_Multi_Store_Online_Marketplace.Pages.AdminNotificationsModel
@{
    Layout = null;
}
@* This page only exists to expose the OnGetListAsync / OnPostMarkAllReadAsync handlers
   used by the admin top bar's notification dropdown (see _Layout.cshtml).
   It is never rendered directly. *@
