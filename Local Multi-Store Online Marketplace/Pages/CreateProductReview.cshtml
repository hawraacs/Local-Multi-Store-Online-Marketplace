@page "{productId:int}"
@model Local_Multi_Store_Online_Marketplace.Pages.CreateProductReviewModel

@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Review Product</title>

    <style>
        body {
            font-family: Arial;
            background: #f5f5f5;
            margin: 0;
        }

        .container {
            max-width: 500px;
            margin: 60px auto;
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.1);
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 15px;
        }

        label {
            display: block;
            font-weight: bold;
            margin-bottom: 6px;
        }

        input, textarea {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 14px;
        }

        textarea {
            resize: none;
            height: 100px;
        }

        .btn {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 10px;
            background: #2563eb;
            color: white;
            font-size: 15px;
            font-weight: bold;
            cursor: pointer;
            margin-top: 10px;
        }

            .btn:hover {
                background: #1e40af;
            }

        .back {
            display: block;
            text-align: center;
            margin-top: 15px;
            color: #666;
            text-decoration: none;
        }

            .back:hover {
                color: black;
            }
    </style>
</head>

<body>

    <div class="container">

        <h2>⭐ Review Product</h2>

        <form method="post">

            <input type="hidden" asp-for="ProductId" />

            <div class="form-group">
                <label>Rating (1 - 5)</label>
                <input asp-for="Rating" type="number" min="1" max="5" />
            </div>

            <div class="form-group">
                <label>Comment</label>
                <textarea asp-for="Comment" placeholder="Write your feedback..."></textarea>
            </div>

            <button type="submit" class="btn">
                Submit Review
            </button>

        </form>

        <a href="/Customer1" class="back">← Back to Feed</a>

    </div>

</body>
</html>